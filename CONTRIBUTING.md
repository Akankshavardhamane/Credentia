# Contributing

Branch from `develop`, use conventional commits, and keep each pull request focused. Before opening it, run `pnpm lint && pnpm typecheck && pnpm test && pnpm build`. Do not commit secrets, raw credential documents, or private keys. Contract changes must include Foundry tests.
