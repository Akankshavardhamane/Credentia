# ADR 001: Vite and Fastify

Vite gives a fast typed frontend loop; Fastify supplies typed, validated API boundaries with structured logging. They remain separately deployable applications in one workspace.
